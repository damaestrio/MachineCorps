/*
Kee-Won Hong - abstracted enemy code for impact.js side scroller
*/

ig.module(
	'game.entities.enemy'
)
.requires(
	'impact.entity'
)

.defines(function(){
EntityEnemy = ig.Entity.extend({
});

});